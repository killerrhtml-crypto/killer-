# Template keystore properties for local signing
# Copy this file to keystore.properties and fill the values. Do NOT commit your actual keystore or passwords.
storeFile=release-key.jks
storePassword=REPLACE_WITH_YOUR_STORE_PASSWORD
keyAlias=release-alias
keyPassword=REPLACE_WITH_YOUR_KEY_PASSWORD
